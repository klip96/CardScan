{
  "name": "cardscan-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android-обёртка (Capacitor WebView) над CardScan PWA — сервер остаётся на офисном ПК в локальной сети.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
